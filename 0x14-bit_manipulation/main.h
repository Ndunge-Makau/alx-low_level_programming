#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>

int _putchar(char c);
unsigned int binary_to_uint(const char *b);
int _strlen(const char *s);
int _power(int x, int y);

#endif /*MAIN_H*/

